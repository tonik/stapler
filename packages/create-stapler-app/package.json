{
  "name": "@tonik/create-stapler-app",
  "version": "0.1.0-alpha.16",
  "main": "./dist/index.mjs",
  "bin": {
    "create-stapler-app": "./dist/index.mjs"
  },
  "publishConfig": {
    "access": "public",
    "tag": "alpha"
  },
  "scripts": {
    "build": "pnpm --filter @tonik/create-stapler-app-core build && pnpm --filter @tonik/create-stapler-app-cli build && tsup index.ts --out-dir dist --format esm"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "gradient-string": "^3.0.0",
    "inquirer": "^10.2.2"
  },
  "devDependencies": {
    "tsup": "^8.2.4",
    "typescript": "^5.6.2"
  }
}
